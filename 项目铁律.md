# 项目铁律 — dev-model-router

> 铁律锚点：**授权 → 备份 → 留痕**

---

## 0. 全局铁律卡继承

本项目无条件继承 DevProjectTeamSkill 全局铁律卡（`references/iron_rules.md`）全部条款，包括但不限于：

- §1 目录访问边界与授权机制
- §2 敏感信息分级处理（A/B/C 三级）
- §3 平台兼容（Windows/macOS 双平台）
- §4 备份与留痕要求
- 其他所有铁律条款

---

## 1. 项目级硬约束

### 1.1 语言铁律

本项目全程中文交流——对话、文档、台账、报告、注释说明一律使用中文；代码标识符可保留英文。

### 1.2 产物落盘铁律

所有生成产物必须落在**本项目目录内**（`/Volumes/BR256G/dev-model-router/`），禁止写入系统盘 / C 盘等非项目路径；确需新建目录只能建在项目目录内且经用户确认。

---

## 2. 访问边界

### 2.1 默认边界

本项目可读写/删除范围 = **本项目所在目录（含子目录）**。

### 2.2 跨项目访问

需要访问本项目目录之外的其他项目目录 / 文件时，一律经 `register_auth` 授权，未填有效期默认仅本次对话有效，会话结束自动失效；跨会话须用户显式指定到期时间并留痕。

### 2.3 安全对齐

系统文件 / 项目外文件操作铁律见全局铁律卡 §1（授权 + 备份 + 留痕三条件齐备，经 `security_audit` 前置审计）。

---

## 3. 违反即阻断

以上铁律条款为强制性约束，任何违反行为将立即阻断当前操作并记录审计。未满足铁律前提条件的环节不得进入下一阶段。

---

**文档版本**：v1.0.0　**写入日期**：2026-09-01
